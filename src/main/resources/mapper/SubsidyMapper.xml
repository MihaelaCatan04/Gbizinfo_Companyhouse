<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.companyhouse.mapper.SubsidyMapper">

    <update id="bulkUpsertSubsidies">
        INSERT INTO subsidy (
        merge_key, date_of_approval, title, amount,
        target, government_departments
        )
        VALUES
        <foreach collection="list" item="e" separator=",">
            (
            #{e.mergeKey}, #{e.dateOfApproval}, #{e.title}, #{e.amount},
            #{e.target}, #{e.governmentDepartments}
            )
        </foreach>
        ON CONFLICT (merge_key) DO NOTHING
    </update>

    <select id="findSubsidyIdByMergeKey" resultType="java.lang.Long">
        SELECT subsidy_id
        FROM subsidy
        WHERE merge_key = #{mergeKey}
    </select>

    <select id="findSubsidyIdsByMergeKeys"
            resultType="com.java.companyhouse.model.solved.MergeKeyIdDto">
        SELECT merge_key AS mergeKey, subsidy_id AS id
        FROM subsidy
        WHERE merge_key IN
        <foreach collection="list" item="mk" open="(" separator="," close=")">
            #{mk}
        </foreach>
    </select>

    <update id="bulkUpsertCompanySubsidies">
        INSERT INTO company_subsidy (
        company_id, subsidy_id,
        inserted_at, updated_at, deleted, sync_id
        )
        VALUES
        <foreach collection="pairs" item="p" separator=",">
            (#{p.companyId}, #{p.childId}, now(), now(), false, #{syncId}::uuid)
        </foreach>
        ON CONFLICT (company_id, subsidy_id) DO UPDATE
        SET deleted = false,
        updated_at = now(),
        sync_id = EXCLUDED.sync_id
    </update>

    <update id="softDeleteAllCompanySubsidies">
        UPDATE company_subsidy
        SET deleted = true,
        updated_at = now()
        WHERE deleted = false
        AND company_id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="softDeleteMissingCompanySubsidies">
        UPDATE company_subsidy
        SET deleted = true,
        updated_at = now()
        WHERE deleted = false
        AND sync_id != #{syncId}::uuid
        AND company_id IN
        <foreach collection="companyIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>