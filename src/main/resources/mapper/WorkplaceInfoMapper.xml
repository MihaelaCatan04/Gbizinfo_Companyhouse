<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.companyhouse.mapper.WorkplaceInfoMapper">

    <update id="bulkUpsertWorkplaceInfos">
        INSERT INTO workplace_info (
        merge_key,
        base_info,
        women_activity_info,
        compatibility_of_childcare_and_work
        )
        VALUES
        <foreach collection="list" item="e" separator=",">
            (#{e.mergeKey}, #{e.baseInfoId}, #{e.womenActivityInfoId}, #{e.compatibilityId})
        </foreach>
        ON CONFLICT (merge_key) DO NOTHING
    </update>

    <update id="bulkAttachCompanyWorkplaceInfos">
        UPDATE company c
        SET workplace_info = v.merge_key,
        updated_at = now()
        FROM (
        VALUES
        <foreach collection="list" item="e" separator=",">
            (#{e.corporateNumber}, #{e.mergeKey})
        </foreach>
        ) AS v(corporate_number, merge_key)
        WHERE c.corporate_number = v.corporate_number
        AND c.workplace_info IS DISTINCT FROM v.merge_key
    </update>

    <update id="bulkClearCompanyWorkplaceInfos">
        UPDATE company
        SET workplace_info = NULL,
        updated_at = now()
        WHERE workplace_info IS NOT NULL
        AND corporate_number IN
        <foreach collection="list" item="cn" open="(" separator="," close=")">
            #{cn}
        </foreach>
    </update>

</mapper>