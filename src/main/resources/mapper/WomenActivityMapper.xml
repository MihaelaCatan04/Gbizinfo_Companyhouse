<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.java.companyhouse.mapper.WomenActivityMapper">

    <update id="bulkUpsertWomenActivities">
        INSERT INTO women_activity_info (
        merge_key,
        female_workers_proportion_type,
        female_workers_proportion,
        female_share_of_manager,
        gender_total_of_manager,
        female_share_of_officers,
        gender_total_of_officers
        ) VALUES
        <foreach collection="list" item="e" separator=",">
            (
            #{e.mergeKey},
            #{e.femaleWorkersProportionType},
            #{e.femaleWorkersProportion},
            #{e.femaleShareOfManager},
            #{e.genderTotalOfManager},
            #{e.femaleShareOfOfficers},
            #{e.genderTotalOfOfficers}
            )
        </foreach>
        ON CONFLICT (merge_key) DO NOTHING
    </update>

    <select id="findWomenActivityIdByMergeKey" resultType="java.lang.Long">
        SELECT women_activity_info_id
        FROM women_activity_info
        WHERE merge_key = #{mergeKey}
    </select>

    <select id="findWomenActivityIdsByMergeKeys"
            resultType="com.java.companyhouse.model.solved.MergeKeyIdDto">
        SELECT merge_key AS mergeKey, women_activity_info_id AS id
        FROM women_activity_info
        WHERE merge_key IN
        <foreach collection="list" item="mk" open="(" separator="," close=")">
            #{mk}
        </foreach>
    </select>

</mapper>